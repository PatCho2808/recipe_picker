<div class="card">
    <div class="card-header">
        Recipes
    </div>
    <div class="card-body">
        <ul>
            <% recipes.forEach(recipe => { %>
                <li>
                    <a class="recipe_link" href=<%=recipe.link%> target="_blank"> <%= recipe.title.toLowerCase()%>  </a>
                </li>
            <% }); %>
            
        </ul>
    </div>
</div>
